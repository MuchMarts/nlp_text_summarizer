To add new training data, first add the respective files to the directory /data
Then add its mapping to cinfig.yaml in the following format:

```
    - orginal: text_file.txt
    summary: summarized_text_file.txt
```